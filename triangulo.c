#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int classificacao (int tipo)
{
    int a, b, c;
    if (a == b == c)
    {
        printf ("equilatero\n");
    }
    else ()
    {
        printf ("escaleno\n");
    }

    return 0;
}
int main() {
    
    int a, b, c;
    
    scanf ("%d%d%d", &a, &b, &c);
    

    printf ("\n", classificacao (tipo));
        

	return 0;
}